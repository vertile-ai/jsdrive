# jsdriver

`jsdriver` is the compatibility package for [Guanine](https://github.com/vertile-ai/jsdrive). It exposes the same API from the canonical `guanine` package.

```ts
import { Browser } from "jsdriver";
```

New projects may install and import `guanine` directly. Both package names are versioned and published together.
